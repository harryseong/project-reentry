import { SubscribeErrorStateMatcher } from './subscribe-error-state-matcher';

describe('SubscribeErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new SubscribeErrorStateMatcher()).toBeTruthy();
  });
});
